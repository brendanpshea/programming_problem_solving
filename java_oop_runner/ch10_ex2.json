{
  "title": "Ch 10.2 — Basic Streams: filter() and forEach()",
  "description": "Practice creating stream pipelines with filter() and forEach().\n\n──────────────────────────────────────────────\nBackground\n──────────────────────────────────────────────\n\nA stream pipeline has three parts:\n  1. Source:              list.stream()\n  2. Intermediate ops:   .filter(predicate)\n  3. Terminal op:        .forEach(lambda)\n\nExample:\n  fleet.stream()\n       .filter(ship -> ship.getSpeed() > 20)\n       .forEach(ship -> System.out.println(ship.getName()));\n\n──────────────────────────────────────────────\nClass: StreamFilterExercise\n──────────────────────────────────────────────\n\nFour sections are marked TODO. In each one, write a stream\npipeline that prints the names of the matching items.\nDo NOT change the System.out.println labels above each TODO.\n\nThe Mermaid and Submarine classes are provided at the bottom\nof the file. Their getters are:\n  Mermaid:   getName(), getOcean()\n  Submarine: getName(), getMaxDepth()\n\n(1) Print names of all mermaids in the \"Atlantic\" ocean.\n\n(2) Print names of submarines whose maxDepth is greater than 500.\n\n(3) Print names of mermaids whose name starts with the letter \"C\".\n    Hint: use  name.startsWith(\"C\")  on the String.\n\n(4) Print names of submarines whose maxDepth is 400 or less\n    (i.e., shallow-water subs).\n\n──────────────────────────────────────────────\nHINTS\n──────────────────────────────────────────────\n• Start every pipeline with  list.stream()\n• .filter() takes a lambda that returns true/false.\n• .forEach() takes a lambda that does something with each item.\n• To print just the name:  .forEach(m -> System.out.println(m.getName()))\n• Each TODO needs ONE stream pipeline — no loops needed.",
  "student_filename": "StreamFilterExercise.java",
  "starter_code": "import java.util.ArrayList;\n\npublic class StreamFilterExercise {\n\n    public static void main(String[] args) {\n\n        ArrayList<Mermaid> mermaids = new ArrayList<>();\n        mermaids.add(new Mermaid(\"Ariel\",  \"Atlantic\"));\n        mermaids.add(new Mermaid(\"Marina\", \"Pacific\"));\n        mermaids.add(new Mermaid(\"Coral\",  \"Atlantic\"));\n        mermaids.add(new Mermaid(\"Pearl\",  \"Indian\"));\n        mermaids.add(new Mermaid(\"Cleo\",   \"Pacific\"));\n\n        ArrayList<Submarine> subs = new ArrayList<>();\n        subs.add(new Submarine(\"Nautilus\",     400));\n        subs.add(new Submarine(\"Deep Explorer\",800));\n        subs.add(new Submarine(\"Sea Voyager\",  300));\n        subs.add(new Submarine(\"Mariana\",     1000));\n\n        // (1) Atlantic mermaids\n        System.out.println(\"Atlantic mermaids:\");\n        // TODO\n\n        // (2) Submarines that can dive deeper than 500 m\n        System.out.println(\"Deep-diving submarines (maxDepth > 500):\");\n        // TODO\n\n        // (3) Mermaids whose name starts with 'C'\n        System.out.println(\"Mermaids with names starting with C:\");\n        // TODO\n\n        // (4) Shallow-water submarines (maxDepth <= 400)\n        System.out.println(\"Shallow-water submarines (maxDepth <= 400):\");\n        // TODO\n    }\n}\n\nclass Mermaid {\n    private String name;\n    private String ocean;\n    public Mermaid(String name, String ocean) {\n        this.name = name; this.ocean = ocean;\n    }\n    public String getName()  { return name; }\n    public String getOcean() { return ocean; }\n}\n\nclass Submarine {\n    private String name;\n    private int maxDepth;\n    public Submarine(String name, int maxDepth) {\n        this.name = name; this.maxDepth = maxDepth;\n    }\n    public String getName()    { return name; }\n    public int getMaxDepth()   { return maxDepth; }\n}\n",
  "support_files": [],
  "test_filename": "TestRunner.java",
  "test_code": "import java.util.ArrayList;\nimport java.util.List;\nimport java.util.stream.Collectors;\n\npublic class TestRunner {\n    static int passed = 0;\n    static int failed = 0;\n\n    public static void main(String[] args) {\n\n        ArrayList<Mermaid> mermaids = new ArrayList<>();\n        mermaids.add(new Mermaid(\"Ariel\",  \"Atlantic\"));\n        mermaids.add(new Mermaid(\"Marina\", \"Pacific\"));\n        mermaids.add(new Mermaid(\"Coral\",  \"Atlantic\"));\n        mermaids.add(new Mermaid(\"Pearl\",  \"Indian\"));\n        mermaids.add(new Mermaid(\"Cleo\",   \"Pacific\"));\n\n        ArrayList<Submarine> subs = new ArrayList<>();\n        subs.add(new Submarine(\"Nautilus\",     400));\n        subs.add(new Submarine(\"Deep Explorer\",800));\n        subs.add(new Submarine(\"Sea Voyager\",  300));\n        subs.add(new Submarine(\"Mariana\",     1000));\n\n        // ── (1) Atlantic mermaids ──\n        List<String> atlanticNames = mermaids.stream()\n            .filter(m -> m.getOcean().equals(\"Atlantic\"))\n            .map(m -> m.getName())\n            .collect(Collectors.toList());\n        checkBool(\"Atlantic count is 2\", true, atlanticNames.size() == 2);\n        checkBool(\"Atlantic contains Ariel\", true, atlanticNames.contains(\"Ariel\"));\n        checkBool(\"Atlantic contains Coral\", true, atlanticNames.contains(\"Coral\"));\n        checkBool(\"Atlantic does NOT contain Marina\", false, atlanticNames.contains(\"Marina\"));\n\n        // ── (2) Deep submarines (maxDepth > 500) ──\n        List<String> deepNames = subs.stream()\n            .filter(s -> s.getMaxDepth() > 500)\n            .map(s -> s.getName())\n            .collect(Collectors.toList());\n        checkBool(\"Deep sub count is 2\", true, deepNames.size() == 2);\n        checkBool(\"Deep contains Deep Explorer\", true, deepNames.contains(\"Deep Explorer\"));\n        checkBool(\"Deep contains Mariana\",       true, deepNames.contains(\"Mariana\"));\n        checkBool(\"Deep does NOT contain Nautilus\", false, deepNames.contains(\"Nautilus\"));\n\n        // ── (3) Names starting with C ──\n        List<String> cNames = mermaids.stream()\n            .filter(m -> m.getName().startsWith(\"C\"))\n            .map(m -> m.getName())\n            .collect(Collectors.toList());\n        checkBool(\"C-names count is 2\", true, cNames.size() == 2);\n        checkBool(\"C-names contains Coral\", true, cNames.contains(\"Coral\"));\n        checkBool(\"C-names contains Cleo\",  true, cNames.contains(\"Cleo\"));\n        checkBool(\"C-names does NOT contain Ariel\", false, cNames.contains(\"Ariel\"));\n\n        // ── (4) Shallow subs (maxDepth <= 400) ──\n        List<String> shallowNames = subs.stream()\n            .filter(s -> s.getMaxDepth() <= 400)\n            .map(s -> s.getName())\n            .collect(Collectors.toList());\n        checkBool(\"Shallow count is 2\", true, shallowNames.size() == 2);\n        checkBool(\"Shallow contains Nautilus\",  true, shallowNames.contains(\"Nautilus\"));\n        checkBool(\"Shallow contains Sea Voyager\",true, shallowNames.contains(\"Sea Voyager\"));\n        checkBool(\"Shallow does NOT contain Mariana\", false, shallowNames.contains(\"Mariana\"));\n\n        // ── summary ──\n        System.out.println();\n        int total = passed + failed;\n        System.out.println((passed == total ? \"PASS\" : \"FAIL\")\n            + \": \" + passed + \"/\" + total + \" tests passed\");\n    }\n\n    static void checkBool(String label, boolean expected, boolean actual) {\n        if (expected == actual) {\n            System.out.println(\"PASS: \" + label); passed++;\n        } else {\n            System.out.println(\"FAIL: \" + label\n                + \" \\u2014 expected \" + expected + \" but got \" + actual); failed++;\n        }\n    }\n}\n"
}
